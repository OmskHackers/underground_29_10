version: '3'

services:
  redis:
    image: redis:latest
    ports:
      - "6379:6379"
    command:
      - "--appendonly"
      - "no"
  server:
      build: .
      ports:
      - "8080:8080"
      restart: on-failure
    