#include "message.h"

/*

extern const MessageController controller1;
extern const MessageController controller2;
extern const MessageController controller5;
extern const MessageController controller7;

static const MessageController* messageTypeToController(unsigned int type, char** error) {
    switch (type) {
    case 1:
    case 2:
        return &controller1;
    case 3:
        return &controller2;
    }

    //thtk_error_new(error, "no module found for version %u", type);
    return NULL;
}

Message* messageOpen(unsigned int type, char* input, errno_t** error) {
    Message* message;
    return message;
}

*/